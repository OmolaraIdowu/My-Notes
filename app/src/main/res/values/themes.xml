<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.MyNotes" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MyNotes" parent="Base.Theme.MyNotes" />

    <style name="SearchViewStyle" parent="Widget.Material3.SearchView">
        <item name="strokeWidth">0dp</item>
        <item name="strokeColor">@android:color/transparent</item>
        <item name="fontFamily">@font/poppins_medium</item>
    </style>

    <style name="RoundedIconTextButtonStyle" parent="Widget.MaterialComponents.Button.TextButton.Icon">
        <item name="cornerRadius">20dp</item>
        <item name="icon">@drawable/add_icon</item>
        <item name="iconGravity">textStart</item>
        <item name="iconTint">@color/black</item>
    </style>

    <style name="BottomSheetDialogTheme" parent="Theme.Material3.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/BottomSheetStyle</item>
    </style>

    <style name="BottomSheetStyle" parent="Widget.Material3.BottomSheet">
        <item name="android:background">@android:color/transparent</item>
    </style>
</resources>